const routes = {
  // 主要页面部分
  mainPages: {
    index: "/pages/index/index",
    about: "/pages/about/index",
  },

  // 子页面部分
  subPages: {
    index: {
      likePage: "/pages/subPages/index/like/index",
    },
  },
};

export default routes;
